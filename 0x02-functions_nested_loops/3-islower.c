#include "main.h"
/**
 * _islower - Enter point
 *
 * c: letter to being tested
 *
 * Return: Alwys 0 (Success)
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
